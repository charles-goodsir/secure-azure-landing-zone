# TODO: configure remote state in an Azure Storage Account (azurerm backend block)
# Don't hardcode the storage account/container names here if you want this reusable —
# consider using a backend config file passed at `terraform init -backend-config=...`
